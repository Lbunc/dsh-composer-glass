# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Build scratch
dist/
*.tgz

# Local notes and investigation records
docs/

# npm cache, redirected into the workspace because the system cache directory is
# not writable in this environment. Only needed for the local `npm pack` /
# `npm publish` dry runs.
.npm-cache/
